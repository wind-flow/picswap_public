{
  "rules": {
    ".read": true,  // 2023-6-21
    ".write": true,  // 2023-6-21
      "rooms": {
      ".indexOn": "roomId",
      "$roomId": {
      	".read": true,  // 적절한 읽기 규칙 설정
        ".write": true, // 적절한 쓰기 규칙 설정
      }
    }
  }
}
